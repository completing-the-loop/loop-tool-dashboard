-r base.txt

# Use URL syntax for database config (required for heroku)
#dj-database-url


# ---------------------------
# WSGI lib

# stand
#gunicorn

# See https://github.com/etianen/django-herokuapp/issues/9#issuecomment-14165240
# waitress caps the number of threads so on heroku is preferred over gunicorn
# waitress

# WSGI lib
Werkzeug

# ---------------------------
# Static file serving in django
#
# Remove if nginx/apache/etc is doing it
whitenoise
